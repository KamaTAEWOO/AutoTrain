# KTX 자동 조회/예약 테스트 앱 - UI/UX 명세서

---

## 1. 개요

### 1.1 목적
Flutter 기반 KTX 자동 조회/예약 테스트 앱의 3개 화면(검색/모니터/결과)과 공통 위젯, 네비게이션, 테마 컬러를 정의한다.

### 1.2 디자인 원칙
- **Material 3** 디자인 시스템 준수
- **코레일 브랜드 아이덴티티** 반영 (블루 계열 Primary)
- **상태 가시성 극대화**: 현재 앱 상태를 언제나 명확히 인지할 수 있도록 색상/아이콘/텍스트 삼중 표현
- **단일 사용자 최적화**: 복잡한 온보딩 없이 즉시 사용 가능한 개발자 친화적 레이아웃

### 1.3 참조 문서
- 기능 명세서: `docs/feature_spec.md`
- WBS: `docs/wbs.md`

---

## 2. 테마 및 컬러 시스템

### 2.1 Material 3 테마

```dart
// 코레일 블루 기반 시드 컬러
seedColor: Color(0xFF003DA5)  // 코레일 공식 블루
```

### 2.2 브랜드 컬러 팔레트

| 토큰명 | Hex | 용도 |
|--------|-----|------|
| `korailBlue` | `#003DA5` | Primary, AppBar, 주요 버튼 |
| `korailBlueDark` | `#002B75` | Primary Container, 강조 영역 |
| `korailBlueLight` | `#E8EFF9` | Surface Variant, 배경 영역 |

### 2.3 시맨틱 컬러 (상태별)

| 상태 | 컬러 토큰 | Hex | 아이콘 | 텍스트 라벨 |
|------|-----------|-----|--------|-------------|
| `idle` | `statusIdle` | `#9E9E9E` (Grey 500) | `Icons.hourglass_empty` | "대기 중" |
| `searching` | `statusSearching` | `#1976D2` (Blue 700) | `Icons.search` | "조회 중..." |
| `found` | `statusFound` | `#388E3C` (Green 700) | `Icons.check_circle` | "열차 발견" |
| `reserving` | `statusReserving` | `#F57C00` (Orange 700) | `Icons.sync` | "예약 중..." |
| `success` | `statusSuccess` | `#2E7D32` (Green 800) | `Icons.done_all` | "예약 성공" |
| `failure` | `statusFailure` | `#D32F2F` (Red 700) | `Icons.error` | "예약 실패" |

### 2.4 텍스트 스타일

| 용도 | TextStyle | 비고 |
|------|-----------|------|
| 화면 제목 | `titleLarge` | AppBar 내부 |
| 섹션 제목 | `titleMedium` + `fontWeight: w600` | 카드 헤더 |
| 본문 | `bodyLarge` | 일반 텍스트 |
| 보조 텍스트 | `bodySmall` + `colorScheme.onSurfaceVariant` | 타임스탬프, 부가 정보 |
| 상태 라벨 | `labelLarge` + 시맨틱 컬러 | StatusBadge 내부 |
| 로그 항목 | `bodySmall` + `fontFamily: monospace` | LogTile 내부 |

### 2.5 간격 및 크기 상수

```
Spacing:
  xs:  4.0
  sm:  8.0
  md: 16.0
  lg: 24.0
  xl: 32.0

BorderRadius:
  card: 12.0
  button: 8.0
  badge: 20.0

Elevation:
  card: 1.0
  activeCard: 3.0
```

---

## 3. 네비게이션 구조

### 3.1 BottomNavigationBar (3탭)

```
┌─────────────────────────────────────────┐
│                                         │
│           [Active Screen]               │
│                                         │
├────────────┬────────────┬───────────────┤
│  🔍 검색   │  📡 모니터  │  📋 결과      │
│  Search    │  Monitor   │  Result       │
└────────────┴────────────┴───────────────┘
```

| 탭 인덱스 | 라벨 | 아이콘 (비활성) | 아이콘 (활성) | 화면 |
|-----------|------|----------------|--------------|------|
| 0 | "검색" | `Icons.search_outlined` | `Icons.search` | SearchScreen |
| 1 | "모니터" | `Icons.monitor_heart_outlined` | `Icons.monitor_heart` | MonitorScreen |
| 2 | "결과" | `Icons.assignment_outlined` | `Icons.assignment` | ResultScreen |

### 3.2 자동 탭 전환 규칙

| 트리거 이벤트 | 전환 대상 탭 | 조건 |
|--------------|-------------|------|
| "조회 시작" 버튼 탭 | 탭 1 (모니터) | 검색 조건 유효성 통과 시 |
| 상태가 `success`로 전환 | 탭 2 (결과) | 예약 성공 시 |
| 상태가 `failure`로 전환 | 탭 2 (결과) | 예약 실패 시 |
| "재시도" 버튼 탭 | 탭 1 (모니터) | 결과 화면에서 재시도 시 |
| "조건 수정" 버튼 탭 | 탭 0 (검색) | 결과 화면에서 조건 변경 시 |

### 3.3 위젯 트리 (앱 루트)

```
MaterialApp
└── ThemeData (Material 3, seedColor: korailBlue)
    └── Scaffold
        ├── body: IndexedStack
        │   ├── [0] SearchScreen
        │   ├── [1] MonitorScreen
        │   └── [2] ResultScreen
        └── bottomNavigationBar: NavigationBar
            ├── NavigationDestination (검색)
            ├── NavigationDestination (모니터)
            └── NavigationDestination (결과)
```

---

## 4. 화면 1: SearchScreen (설정/검색 화면)

### 4.1 화면 목적
사용자가 열차 검색 조건(출발역, 도착역, 날짜, 시간)을 입력하고, 자동 예약 옵션을 설정한 뒤 조회를 시작하는 진입 화면.

### 4.2 화면 레이아웃

```
┌─────────────────────────────────┐
│  AppBar: "KTX 자동 예약"         │
├─────────────────────────────────┤
│                                 │
│  ┌─ StationSelector ──────────┐ │
│  │ 출발역: [서울 ▼]            │ │
│  │        ⇅ (역 교환 버튼)     │ │
│  │ 도착역: [부산 ▼]            │ │
│  └────────────────────────────┘ │
│                                 │
│  ┌─ 날짜/시간 ────────────────┐ │
│  │ 📅 날짜  [2026-02-05]  ▶  │ │
│  │ 🕐 시간  [09:00]       ▶  │ │
│  └────────────────────────────┘ │
│                                 │
│  ┌─ 자동 예약 설정 ───────────┐ │
│  │ 자동 예약        [🔵 ON ]  │ │
│  │ 조회 주기    ──●── 10초    │ │
│  │              5s        30s │ │
│  └────────────────────────────┘ │
│                                 │
│  ┌─────────────────────────────┐│
│  │      [ 🔍 조회 시작 ]       ││
│  └─────────────────────────────┘│
│                                 │
├────────────┬──────────┬─────────┤
│   검색     │  모니터   │  결과   │
└────────────┴──────────┴─────────┘
```

### 4.3 위젯 상세

#### 4.3.1 StationSelector (역 선택 영역)

- **출발역/도착역 입력**: `Autocomplete<String>` 위젯 사용
  - 데이터소스: KTX 정차역 목록 (하드코딩 리스트)
  - 타이핑 시 실시간 필터링 (초성 검색 지원은 향후 고려)
  - 선택 후 TextFormField에 역명 표시
- **역 교환 버튼**: `IconButton(Icons.swap_vert)`
  - 출발역과 도착역 값 스왑
  - 애니메이션: `AnimatedSwitcher` 또는 간단한 스왑 전환

#### 4.3.2 날짜 선택

- **위젯**: `ListTile` 탭 시 `showDatePicker()` 호출
- **표시 형식**: `yyyy-MM-dd` (예: 2026-02-05)
- **제약**: `firstDate` = 오늘, `lastDate` = 오늘 + 30일
- **기본값**: 오늘 날짜

#### 4.3.3 시간 선택

- **위젯**: `ListTile` 탭 시 `showTimePicker()` 호출
- **표시 형식**: `HH:mm` (24시간제, 예: 09:00)
- **기본값**: 현재 시각의 다음 정시

#### 4.3.4 자동 예약 설정

- **자동 예약 ON/OFF**: `SwitchListTile`
  - ON: 좌석 발견 시 자동으로 예약 시도
  - OFF: 좌석 발견 시 알림만 표시 (수동 예약)
  - 기본값: ON
- **조회 주기**: `Slider`
  - 범위: 5초 ~ 30초
  - divisions: 5 (5초 단위: 5, 10, 15, 20, 25, 30)
  - 기본값: 10초
  - 라벨: 현재 값 표시 (`${value.toInt()}초`)
  - 비활성 조건: 자동 예약이 OFF이면 `Slider` 비활성 (그레이아웃)

#### 4.3.5 조회 시작 버튼

- **위젯**: `SizedBox(width: double.infinity)` > `ElevatedButton.icon`
- **아이콘**: `Icons.search`
- **텍스트**: "조회 시작"
- **스타일**: Primary 컬러, 높이 52px, borderRadius 8
- **비활성 조건**: 출발역 또는 도착역이 비어있을 때
- **탭 동작**:
  1. 입력값 유효성 검증
  2. SearchCondition 객체 생성
  3. 모니터 화면(탭 1)으로 자동 전환
  4. 자동 조회 타이머 시작

### 4.4 위젯 트리 (pseudocode)

```
SearchScreen (ConsumerStatefulWidget)
└── Scaffold
    ├── appBar: AppBar
    │   └── title: Text("KTX 자동 예약")
    └── body: SingleChildScrollView
        └── Padding (md: 16)
            └── Column (spacing: lg: 24)
                │
                ├── Card (StationSelector 영역)
                │   └── Padding (md: 16)
                │       └── Column
                │           ├── Text("구간 선택", style: titleMedium)
                │           ├── SizedBox(h: sm)
                │           ├── Autocomplete<String>  ← 출발역
                │           │   └── TextFormField
                │           │       ├── decoration: InputDecoration
                │           │       │   ├── labelText: "출발역"
                │           │       │   ├── prefixIcon: Icon(Icons.trip_origin)
                │           │       │   └── border: OutlineInputBorder
                │           │       └── validator: 빈 값 체크
                │           ├── Center
                │           │   └── IconButton(Icons.swap_vert)  ← 역 교환
                │           └── Autocomplete<String>  ← 도착역
                │               └── TextFormField
                │                   ├── decoration: InputDecoration
                │                   │   ├── labelText: "도착역"
                │                   │   ├── prefixIcon: Icon(Icons.location_on)
                │                   │   └── border: OutlineInputBorder
                │                   └── validator: 빈 값 체크
                │
                ├── Card (날짜/시간 영역)
                │   └── Padding (md: 16)
                │       └── Column
                │           ├── Text("날짜 / 시간", style: titleMedium)
                │           ├── SizedBox(h: sm)
                │           ├── ListTile  ← 날짜 선택
                │           │   ├── leading: Icon(Icons.calendar_today)
                │           │   ├── title: Text("날짜")
                │           │   ├── subtitle: Text(selectedDate)
                │           │   ├── trailing: Icon(Icons.chevron_right)
                │           │   └── onTap: showDatePicker()
                │           ├── Divider
                │           └── ListTile  ← 시간 선택
                │               ├── leading: Icon(Icons.access_time)
                │               ├── title: Text("시간")
                │               ├── subtitle: Text(selectedTime)
                │               ├── trailing: Icon(Icons.chevron_right)
                │               └── onTap: showTimePicker()
                │
                ├── Card (자동 예약 설정 영역)
                │   └── Padding (md: 16)
                │       └── Column
                │           ├── Text("자동 예약 설정", style: titleMedium)
                │           ├── SizedBox(h: sm)
                │           ├── SwitchListTile  ← 자동 예약 토글
                │           │   ├── title: Text("자동 예약")
                │           │   ├── subtitle: Text("좌석 발견 시 자동으로 예약을 시도합니다")
                │           │   ├── value: autoReserve
                │           │   └── onChanged: (v) => setState
                │           ├── Divider
                │           ├── ListTile
                │           │   ├── title: Text("조회 주기")
                │           │   └── trailing: Text("${interval}초", style: titleMedium)
                │           └── Slider  ← 조회 주기
                │               ├── value: interval
                │               ├── min: 5, max: 30, divisions: 5
                │               ├── label: "${interval.toInt()}초"
                │               ├── enabled: autoReserve == true
                │               └── onChanged: (v) => setState
                │
                └── SizedBox(width: double.infinity)
                    └── ElevatedButton.icon  ← 조회 시작
                        ├── icon: Icon(Icons.search)
                        ├── label: Text("조회 시작")
                        ├── style: ElevatedButton.styleFrom
                        │   ├── minimumSize: Size(double.infinity, 52)
                        │   └── shape: RoundedRectangleBorder(borderRadius: 8)
                        └── onPressed: _startSearch (null if invalid)
```

### 4.5 인터랙션 및 유효성 검증

| 필드 | 검증 규칙 | 에러 메시지 |
|------|----------|-------------|
| 출발역 | 빈 값 불허, 유효한 역명 | "출발역을 선택해주세요" |
| 도착역 | 빈 값 불허, 유효한 역명 | "도착역을 선택해주세요" |
| 출발역 != 도착역 | 동일 역 불허 | "출발역과 도착역이 같습니다" |
| 날짜 | 오늘 이전 불허 | "과거 날짜는 선택할 수 없습니다" |
| 시간 | 오늘+과거시간 불허 | "이미 지난 시간입니다" |

---

## 5. 화면 2: MonitorScreen (실시간 상태 화면)

### 5.1 화면 목적
자동 조회 진행 상황을 실시간으로 표시하고, 현재 상태/통계/로그를 모니터링하며, 조회를 중지할 수 있는 대시보드 화면.

### 5.2 화면 레이아웃

```
┌─────────────────────────────────┐
│  AppBar: "실시간 모니터"         │
├─────────────────────────────────┤
│                                 │
│  ┌─ StatusCard ───────────────┐ │
│  │  [🔵]  조회 중...           │ │
│  │                            │ │
│  │  서울 → 부산               │ │
│  │  2026-02-05  09:00 이후    │ │
│  │  자동 예약: ON  주기: 10초  │ │
│  └────────────────────────────┘ │
│                                 │
│  ┌─ StatsRow ─────────────────┐ │
│  │  조회 횟수    마지막 조회    │ │
│  │    23회       14:32:05     │ │
│  └────────────────────────────┘ │
│                                 │
│  ┌─────────────────────────────┐│
│  │      [ ■ 중지 ]             ││
│  └─────────────────────────────┘│
│                                 │
│  ┌─ LogListView ──────────────┐ │
│  │  14:32:05  조회 #23 좌석없음│ │
│  │  14:31:55  조회 #22 좌석없음│ │
│  │  14:31:45  조회 #21 좌석없음│ │
│  │  14:31:35  조회 #20 좌석없음│ │
│  │  ...                       │ │
│  └────────────────────────────┘ │
│                                 │
├────────────┬──────────┬─────────┤
│   검색     │  모니터   │  결과   │
└────────────┴──────────┴─────────┘
```

### 5.3 위젯 상세

#### 5.3.1 StatusCard (상태 카드)

- **위젯**: `Card` + `AnimatedContainer`
- **구성**:
  - `StatusBadge`: 현재 상태 표시 (아이콘 + 색상 + 라벨)
  - 검색 조건 요약: 출발역 > 도착역, 날짜, 시간
  - 설정 요약: 자동 예약 여부, 조회 주기
- **상태별 외관 변화**:

| 상태 | 카드 테두리 색 | 배경색 (Surface) | 아이콘 애니메이션 |
|------|---------------|-----------------|------------------|
| `idle` | Grey 300 | Surface | 없음 |
| `searching` | Blue 700 | Blue 50 | 아이콘 회전 (`AnimatedRotation`) |
| `found` | Green 700 | Green 50 | 없음 |
| `reserving` | Orange 700 | Orange 50 | 펄스 (`AnimatedScale` 반복) |
| `success` | Green 800 | Green 50 | 없음 |
| `failure` | Red 700 | Red 50 | 없음 |

#### 5.3.2 StatsRow (통계 행)

- **위젯**: `Card` > `Row` > 2개 `Expanded` 컬럼
- **좌측 항목**: 조회 횟수
  - 라벨: "조회 횟수" (`bodySmall`, onSurfaceVariant)
  - 값: "23회" (`headlineSmall`, bold)
- **우측 항목**: 마지막 조회 시간
  - 라벨: "마지막 조회" (`bodySmall`, onSurfaceVariant)
  - 값: "14:32:05" (`headlineSmall`, bold)
- **구분선**: `VerticalDivider` 또는 `Container(width: 1)`

#### 5.3.3 중지 버튼

- **위젯**: `SizedBox(width: double.infinity)` > `OutlinedButton.icon`
- **아이콘**: `Icons.stop_circle_outlined`
- **텍스트**: "중지"
- **스타일**: `colorScheme.error` 테두리/텍스트, 높이 48px
- **상태**:
  - `idle` 상태: 비활성 (disabled)
  - `searching` / `reserving` 상태: 활성 (enabled, 빨간 강조)
  - `success` / `failure` 상태: "새로 시작"으로 텍스트 변경
- **탭 동작**:
  1. 타이머 중지
  2. 상태를 `idle`로 전환
  3. 로그에 "사용자에 의해 중지됨" 기록

#### 5.3.4 LogListView (로그 목록)

- **위젯**: `Expanded` > `Card` > `ListView.builder`
- **헤더**: `ListTile(title: "조회 로그", trailing: Text("${logCount}건"))`
- **항목**: `LogTile` 위젯 (5.7절 공통 위젯 참조)
- **정렬**: 최신 로그가 상단 (역순)
- **최대 표시**: 최근 100건 (전체 로그는 ResultScreen에서 확인)
- **자동 스크롤**: 새 로그 추가 시 자동으로 상단 스크롤

### 5.4 위젯 트리 (pseudocode)

```
MonitorScreen (ConsumerWidget)
└── Scaffold
    ├── appBar: AppBar
    │   └── title: Text("실시간 모니터")
    └── body: Padding (md: 16)
        └── Column
            │
            ├── StatusCard  ← 상태 카드
            │   └── AnimatedContainer (duration: 300ms)
            │       └── Card
            │           └── Padding (md: 16)
            │               └── Column
            │                   ├── Row (mainAxisAlignment: spaceBetween)
            │                   │   ├── StatusBadge(status)  ← 공통 위젯
            │                   │   └── if (searching) SizedBox(18x18)
            │                   │       └── CircularProgressIndicator(strokeWidth: 2)
            │                   ├── SizedBox(h: md)
            │                   ├── Row  ← 구간 표시
            │                   │   ├── Icon(Icons.train, size: 20)
            │                   │   ├── SizedBox(w: sm)
            │                   │   └── Text("서울 → 부산", style: titleMedium)
            │                   ├── SizedBox(h: xs)
            │                   ├── Row  ← 날짜/시간 표시
            │                   │   ├── Icon(Icons.event, size: 20)
            │                   │   ├── SizedBox(w: sm)
            │                   │   └── Text("2026-02-05  09:00 이후")
            │                   └── SizedBox(h: xs)
            │                       └── Row  ← 설정 표시
            │                           ├── Chip(label: "자동 예약 ON")
            │                           ├── SizedBox(w: sm)
            │                           └── Chip(label: "10초 주기")
            │
            ├── SizedBox(h: md)
            │
            ├── Card (StatsRow)  ← 통계 행
            │   └── Padding (md: 16)
            │       └── IntrinsicHeight
            │           └── Row
            │               ├── Expanded
            │               │   └── Column (crossAxis: center)
            │               │       ├── Text("조회 횟수", style: bodySmall)
            │               │       └── Text("${count}회", style: headlineSmall)
            │               ├── VerticalDivider
            │               └── Expanded
            │                   └── Column (crossAxis: center)
            │                       ├── Text("마지막 조회", style: bodySmall)
            │                       └── Text(lastTime, style: headlineSmall)
            │
            ├── SizedBox(h: md)
            │
            ├── SizedBox(width: double.infinity)  ← 중지 버튼
            │   └── OutlinedButton.icon
            │       ├── icon: Icon(Icons.stop_circle_outlined)
            │       ├── label: Text("중지")
            │       ├── style: OutlinedButton.styleFrom
            │       │   ├── foregroundColor: colorScheme.error
            │       │   ├── side: BorderSide(color: colorScheme.error)
            │       │   └── minimumSize: Size(double.infinity, 48)
            │       └── onPressed: _stopMonitoring (null if idle)
            │
            ├── SizedBox(h: md)
            │
            └── Expanded  ← 로그 목록 (남은 공간 전부 사용)
                └── Card
                    └── Column
                        ├── ListTile
                        │   ├── title: Text("조회 로그", style: titleMedium)
                        │   └── trailing: Text("${logs.length}건")
                        ├── Divider(h: 1)
                        └── Expanded
                            └── ListView.builder
                                ├── reverse: false
                                ├── itemCount: logs.length
                                └── itemBuilder: (context, index)
                                    └── LogTile(log: logs[index])  ← 공통 위젯
```

### 5.5 실시간 업데이트 동작

| 이벤트 | UI 업데이트 |
|--------|------------|
| 타이머 틱 (조회 실행) | StatsRow 조회 횟수 +1, 마지막 조회 시간 갱신 |
| 조회 결과 수신 (좌석 없음) | LogListView에 "좌석없음" 로그 추가 |
| 좌석 발견 | StatusCard 상태 `found`로 전환, 로그 추가 |
| 예약 시도 시작 | StatusCard 상태 `reserving`으로 전환 |
| 예약 성공 | StatusCard 상태 `success`, 자동으로 ResultScreen 전환 |
| 예약 실패 | StatusCard 상태 `failure`, 자동으로 ResultScreen 전환 |
| 네트워크 오류 | 로그에 오류 기록, StatusCard에 경고 아이콘 표시 |
| 백오프 적용 | StatsRow 하단에 "백오프: 다음 조회 Ns 후" 표시 |

---

## 6. 화면 3: ResultScreen (예약 결과 화면)

### 6.1 화면 목적
예약 시도의 최종 결과(성공/실패)를 명확하게 표시하고, 성공 시 결제 안내, 실패 시 재시도 옵션을 제공하며, 전체 로그를 확인할 수 있는 화면.

### 6.2 화면 레이아웃 - 성공 시

```
┌─────────────────────────────────┐
│  AppBar: "예약 결과"             │
├─────────────────────────────────┤
│                                 │
│  ┌─ ReservationCard ──────────┐ │
│  │        ✅ 예약 성공          │ │
│  │                            │ │
│  │  예약번호: 12345678        │ │
│  │  ───────────────────────── │ │
│  │  열차: KTX-101             │ │
│  │  구간: 서울 → 부산          │ │
│  │  출발: 09:00  도착: 11:30  │ │
│  │  날짜: 2026-02-05          │ │
│  │  ───────────────────────── │ │
│  │  ⚠️ 10분 내 결제가 필요합니다│ │
│  │  미결제 시 예약이 자동       │ │
│  │  취소됩니다.                │ │
│  └────────────────────────────┘ │
│                                 │
│  ┌─ 액션 버튼 ────────────────┐ │
│  │  [ 📋 예약번호 복사 ]       │ │
│  │  [ 🔍 새로운 조회 ]         │ │
│  └────────────────────────────┘ │
│                                 │
│  ▶ 전체 로그 (23건)      [펼치기]│
│                                 │
├────────────┬──────────┬─────────┤
│   검색     │  모니터   │  결과   │
└────────────┴──────────┴─────────┘
```

### 6.3 화면 레이아웃 - 실패 시

```
┌─────────────────────────────────┐
│  AppBar: "예약 결과"             │
├─────────────────────────────────┤
│                                 │
│  ┌─ ReservationCard ──────────┐ │
│  │        ❌ 예약 실패          │ │
│  │                            │ │
│  │  실패 사유:                 │ │
│  │  "해당 열차의 좌석이 이미   │ │
│  │   매진되었습니다."          │ │
│  │                            │ │
│  │  시도한 열차: KTX-101      │ │
│  │  구간: 서울 → 부산          │ │
│  │  시간: 09:00               │ │
│  └────────────────────────────┘ │
│                                 │
│  ┌─ 액션 버튼 ────────────────┐ │
│  │  [ 🔄 재시도 ]              │ │
│  │  [ ✏️ 조건 수정 ]           │ │
│  └────────────────────────────┘ │
│                                 │
│  ▶ 전체 로그 (23건)      [펼치기]│
│                                 │
├────────────┬──────────┬─────────┤
│   검색     │  모니터   │  결과   │
└────────────┴──────────┴─────────┘
```

### 6.4 화면 레이아웃 - 결과 없음 (초기 상태)

```
┌─────────────────────────────────┐
│  AppBar: "예약 결과"             │
├─────────────────────────────────┤
│                                 │
│                                 │
│         (빈 상태 일러스트)       │
│        Icons.inbox_outlined     │
│          (size: 80, grey)       │
│                                 │
│     "아직 예약 결과가 없습니다"   │
│     "검색 화면에서 조회를         │
│      시작해주세요"               │
│                                 │
│    [ 검색 화면으로 이동 ]        │
│                                 │
├────────────┬──────────┬─────────┤
│   검색     │  모니터   │  결과   │
└────────────┴──────────┴─────────┘
```

### 6.5 위젯 상세

#### 6.5.1 ReservationCard - 성공

- **위젯**: `Card` (elevation: 2, borderRadius: 12)
- **테두리**: `Border(left: BorderSide(color: statusSuccess, width: 4))`
- **구성**:
  - 성공 아이콘 + "예약 성공" 텍스트 (Green 800, titleLarge, bold)
  - 예약번호: 큰 텍스트로 강조 (`headlineMedium`, bold)
  - 구분선
  - `TrainCard` 위젯으로 열차 정보 표시 (6.7절 공통 위젯 참조)
  - 구분선
  - 결제 안내 배너:
    - 배경: `Colors.amber.shade50`
    - 아이콘: `Icons.warning_amber_rounded` (Orange)
    - 텍스트: "10분 내 결제가 필요합니다. 미결제 시 예약이 자동 취소됩니다."
    - 스타일: `bodyMedium`, `fontWeight: w500`

#### 6.5.2 ReservationCard - 실패

- **위젯**: `Card` (elevation: 2, borderRadius: 12)
- **테두리**: `Border(left: BorderSide(color: statusFailure, width: 4))`
- **구성**:
  - 실패 아이콘 + "예약 실패" 텍스트 (Red 700, titleLarge, bold)
  - 실패 사유: `Container` (Red 50 배경, padding 12)
    - `Text(failureMessage, style: bodyLarge)`
  - 시도한 열차 정보 (축약 표시)

#### 6.5.3 액션 버튼 영역

- **성공 시**:
  - "예약번호 복사": `OutlinedButton.icon(Icons.copy)` -- 클립보드 복사
  - "새로운 조회": `ElevatedButton.icon(Icons.search)` -- 탭 0(검색)으로 이동
- **실패 시**:
  - "재시도": `ElevatedButton.icon(Icons.refresh)` -- 탭 1(모니터)로 이동, 자동 조회 재개
  - "조건 수정": `OutlinedButton.icon(Icons.edit)` -- 탭 0(검색)으로 이동
- **레이아웃**: `Column` > 2개 `SizedBox(width: double.infinity)` > 버튼 (간격 sm: 8)

#### 6.5.4 LogExpansionTile (전체 로그)

- **위젯**: `Card` > `ExpansionTile`
- **접힌 상태**: "전체 로그 (N건)" + `Icons.expand_more`
- **펼친 상태**: `ListView.builder`로 전체 로그 항목 표시
  - 각 항목: `LogTile` 위젯 (6.7절 공통 위젯 참조)
  - 최대 높이: `ConstrainedBox(maxHeight: 300)`
  - 스크롤: 내부 스크롤 가능
- **기본 상태**: 접힌 상태

### 6.6 위젯 트리 (pseudocode)

```
ResultScreen (ConsumerWidget)
└── Scaffold
    ├── appBar: AppBar
    │   └── title: Text("예약 결과")
    └── body: _buildBody(reservationState)

// --- 결과 없음 (초기) ---
_buildEmptyState()
└── Center
    └── Column (mainAxisAlignment: center)
        ├── Icon(Icons.inbox_outlined, size: 80, color: grey)
        ├── SizedBox(h: md)
        ├── Text("아직 예약 결과가 없습니다", style: titleMedium)
        ├── SizedBox(h: sm)
        ├── Text("검색 화면에서 조회를 시작해주세요", style: bodyMedium)
        ├── SizedBox(h: lg)
        └── ElevatedButton.icon
            ├── icon: Icon(Icons.search)
            ├── label: Text("검색 화면으로 이동")
            └── onPressed: → 탭 0 전환

// --- 성공 ---
_buildSuccessResult(reservation)
└── SingleChildScrollView
    └── Padding (md: 16)
        └── Column (spacing: md: 16)
            │
            ├── ReservationCard (성공)
            │   └── Card (elevation: 2)
            │       └── Container
            │           ├── decoration: BoxDecoration
            │           │   └── border: Border(left: BorderSide(statusSuccess, 4))
            │           └── Padding (md: 16)
            │               └── Column
            │                   ├── Row (mainAxisAlignment: center)
            │                   │   ├── Icon(Icons.check_circle, statusSuccess, 32)
            │                   │   ├── SizedBox(w: sm)
            │                   │   └── Text("예약 성공", titleLarge, bold)
            │                   ├── SizedBox(h: lg)
            │                   ├── Text("예약번호", bodySmall, onSurfaceVariant)
            │                   ├── SelectableText(reservationId, headlineMedium, bold)
            │                   ├── Divider
            │                   ├── TrainCard(train)  ← 공통 위젯
            │                   ├── Divider
            │                   └── Container (amber.shade50, borderRadius: 8)
            │                       └── Padding (md: 12)
            │                           └── Row
            │                               ├── Icon(Icons.warning_amber_rounded, orange)
            │                               ├── SizedBox(w: sm)
            │                               └── Expanded
            │                                   └── Text("10분 내 결제가 필요합니다.\n미결제 시 예약이 자동 취소됩니다.")
            │
            ├── Column (액션 버튼)
            │   ├── SizedBox(width: infinity)
            │   │   └── OutlinedButton.icon
            │   │       ├── icon: Icon(Icons.copy)
            │   │       ├── label: Text("예약번호 복사")
            │   │       └── onPressed: _copyReservationId
            │   ├── SizedBox(h: sm)
            │   └── SizedBox(width: infinity)
            │       └── ElevatedButton.icon
            │           ├── icon: Icon(Icons.search)
            │           ├── label: Text("새로운 조회")
            │           └── onPressed: → 탭 0 전환, 상태 리셋
            │
            └── Card (로그)
                └── ExpansionTile
                    ├── title: Text("전체 로그")
                    ├── subtitle: Text("${logs.length}건")
                    ├── leading: Icon(Icons.list_alt)
                    ├── initiallyExpanded: false
                    └── children:
                        └── ConstrainedBox (maxHeight: 300)
                            └── ListView.builder
                                ├── shrinkWrap: true
                                ├── itemCount: logs.length
                                └── itemBuilder: (context, index)
                                    └── LogTile(log: logs[index])

// --- 실패 ---
_buildFailureResult(reservation)
└── SingleChildScrollView
    └── Padding (md: 16)
        └── Column (spacing: md: 16)
            │
            ├── ReservationCard (실패)
            │   └── Card (elevation: 2)
            │       └── Container
            │           ├── decoration: BoxDecoration
            │           │   └── border: Border(left: BorderSide(statusFailure, 4))
            │           └── Padding (md: 16)
            │               └── Column
            │                   ├── Row (mainAxisAlignment: center)
            │                   │   ├── Icon(Icons.cancel, statusFailure, 32)
            │                   │   ├── SizedBox(w: sm)
            │                   │   └── Text("예약 실패", titleLarge, bold)
            │                   ├── SizedBox(h: md)
            │                   ├── Container (red.shade50, borderRadius: 8, padding: 12)
            │                   │   └── Column (crossAxis: start)
            │                   │       ├── Text("실패 사유", bodySmall, bold)
            │                   │       ├── SizedBox(h: xs)
            │                   │       └── Text(failureMessage, bodyLarge)
            │                   ├── SizedBox(h: md)
            │                   ├── Divider
            │                   └── TrainCard(train, compact: true)  ← 축약 모드
            │
            ├── Column (액션 버튼)
            │   ├── SizedBox(width: infinity)
            │   │   └── ElevatedButton.icon
            │   │       ├── icon: Icon(Icons.refresh)
            │   │       ├── label: Text("재시도")
            │   │       └── onPressed: → 탭 1 전환, 자동 조회 재개
            │   ├── SizedBox(h: sm)
            │   └── SizedBox(width: infinity)
            │       └── OutlinedButton.icon
            │           ├── icon: Icon(Icons.edit)
            │           ├── label: Text("조건 수정")
            │           └── onPressed: → 탭 0 전환
            │
            └── Card (로그)
                └── ExpansionTile
                    ├── (동일 구조)
                    └── ...
```

---

## 7. 공통 위젯

### 7.1 StationSelector

역 선택을 위한 복합 위젯. 출발역/도착역 입력과 교환 버튼을 포함한다.

```
StationSelector (StatefulWidget)
├── Props:
│   ├── onDepartureChanged: (String) → void
│   ├── onArrivalChanged: (String) → void
│   ├── departure: String?
│   └── arrival: String?
│
└── Build:
    └── Card
        └── Padding (md: 16)
            └── Column
                ├── Text("구간 선택", style: titleMedium)
                ├── SizedBox(h: sm)
                ├── Row
                │   ├── Expanded
                │   │   └── Autocomplete<String>
                │   │       ├── optionsBuilder: _filterStations(departure)
                │   │       └── fieldViewBuilder: TextFormField
                │   │           ├── label: "출발역"
                │   │           └── prefixIcon: Icon(Icons.trip_origin, korailBlue)
                │   ├── Padding (horizontal: xs)
                │   │   └── IconButton
                │   │       ├── icon: Icon(Icons.swap_vert)
                │   │       ├── tooltip: "출발역/도착역 교환"
                │   │       └── onPressed: _swapStations
                │   └── Expanded
                │       └── Autocomplete<String>
                │           ├── optionsBuilder: _filterStations(arrival)
                │           └── fieldViewBuilder: TextFormField
                │               ├── label: "도착역"
                │               └── prefixIcon: Icon(Icons.location_on, korailBlue)
                └── (선택적) SizedBox(h: sm)
                    └── Wrap  ← 빠른 선택 칩
                        ├── ActionChip("서울 → 부산")
                        └── ActionChip("서울 → 동대구")
```

**역 목록 데이터** (KTX 주요 정차역):
```
서울, 용산, 광명, 수원, 천안아산, 오송, 대전, 김천구미,
동대구, 경주, 울산, 부산, 익산, 광주송정, 목포, 전주,
여수EXPO, 강릉, 동해, 포항
```

### 7.2 StatusBadge

현재 상태를 색상 + 아이콘 + 텍스트로 표현하는 배지 위젯.

```
StatusBadge (StatelessWidget)
├── Props:
│   ├── status: MonitorStatus (enum)
│   └── size: StatusBadgeSize (small / medium / large)
│
└── Build:
    └── Container
        ├── decoration: BoxDecoration
        │   ├── color: _statusColor(status).withOpacity(0.12)
        │   └── borderRadius: 20
        └── Padding (horizontal: 12, vertical: 6)
            └── Row (mainAxisSize: min)
                ├── Icon(_statusIcon(status), color: _statusColor(status), size: _iconSize)
                ├── SizedBox(w: xs)
                └── Text(_statusLabel(status), color: _statusColor(status), labelLarge)

상태-아이콘-라벨-색상 매핑:
┌────────────┬──────────────────────────┬────────────┬───────────┐
│ Status     │ Icon                     │ Label      │ Color     │
├────────────┼──────────────────────────┼────────────┼───────────┤
│ idle       │ Icons.hourglass_empty    │ "대기 중"   │ Grey 500  │
│ searching  │ Icons.search             │ "조회 중..." │ Blue 700  │
│ found      │ Icons.check_circle       │ "열차 발견"  │ Green 700 │
│ reserving  │ Icons.sync               │ "예약 중..." │ Orange700 │
│ success    │ Icons.done_all           │ "예약 성공"  │ Green 800 │
│ failure    │ Icons.error              │ "예약 실패"  │ Red 700   │
└────────────┴──────────────────────────┴────────────┴───────────┘
```

### 7.3 TrainCard

열차 정보를 표시하는 카드 위젯. 일반 모드와 축약(compact) 모드를 지원한다.

```
TrainCard (StatelessWidget)
├── Props:
│   ├── train: Train
│   └── compact: bool (default: false)
│
└── Build (일반 모드):
    └── Column
        ├── Row (열차 정보 헤더)
        │   ├── Icon(Icons.train, korailBlue)
        │   ├── SizedBox(w: sm)
        │   ├── Text(train.trainNo, titleMedium, bold)  // "KTX-101"
        │   └── Spacer
        │       └── Row
        │           ├── if (train.generalSeats) Chip("일반실", green)
        │           └── if (train.specialSeats) Chip("특실", blue)
        │
        ├── SizedBox(h: md)
        │
        ├── Row (구간 + 시간 타임라인)
        │   ├── Column (crossAxis: center)
        │   │   ├── Text(train.depTime, titleMedium, bold)  // "09:00"
        │   │   └── Text(train.depStation, bodySmall)       // "서울"
        │   ├── Expanded
        │   │   └── Column
        │   │       ├── Divider (with arrow)
        │   │       └── Text(duration, bodySmall, center)   // "2시간 30분"
        │   └── Column (crossAxis: center)
        │       ├── Text(train.arrTime, titleMedium, bold)  // "11:30"
        │       └── Text(train.arrStation, bodySmall)       // "부산"
        │
        └── if (!compact) SizedBox(h: sm)
            └── Text(train.date, bodySmall)                 // "2026-02-05"

└── Build (축약 모드):
    └── Row
        ├── Icon(Icons.train, size: 16)
        ├── SizedBox(w: sm)
        ├── Text("${train.trainNo}", bodyMedium, bold)
        ├── SizedBox(w: sm)
        ├── Text("${train.depStation}→${train.arrStation}", bodyMedium)
        ├── Spacer
        └── Text(train.depTime, bodyMedium)
```

### 7.4 LogTile

개별 로그 항목을 표시하는 타일 위젯.

```
LogTile (StatelessWidget)
├── Props:
│   └── log: LogEntry
│
└── Build:
    └── Padding (horizontal: md, vertical: xs)
        └── Row
            ├── Container (width: 4, height: 36, borderRadius: 2)
            │   └── color: _actionColor(log.action)
            │       // search → Blue, reserve → Orange,
            │       // login → Grey, error → Red
            ├── SizedBox(w: sm)
            ├── SizedBox(width: 72)
            │   └── Text(formatTime(log.timestamp), bodySmall, monospace)
            │       // "14:32:05"
            ├── SizedBox(w: sm)
            ├── _ResultIcon(log.result)
            │   // success → green check (12px)
            │   // failure → red x (12px)
            │   // no_seats → grey dash (12px)
            ├── SizedBox(w: sm)
            └── Expanded
                └── Text(log.detail, bodySmall, overflow: ellipsis, maxLines: 1)
                    // "조회 #23 - 좌석 없음"

액션별 색상:
┌──────────┬───────────┐
│ Action   │ Color     │
├──────────┼───────────┤
│ search   │ Blue 400  │
│ reserve  │ Orange400 │
│ login    │ Grey 400  │
│ error    │ Red 400   │
└──────────┴───────────┘
```

---

## 8. 상태별 화면 전환 흐름

### 8.1 전체 플로우 다이어그램

```
[SearchScreen]                  [MonitorScreen]                [ResultScreen]
     │                               │                              │
     │  "조회 시작" 탭               │                              │
     │──────────────────────────────▶│                              │
     │                               │  상태: searching             │
     │                               │  (타이머 시작, 주기적 조회)   │
     │                               │                              │
     │                               │  좌석 발견                   │
     │                               │  상태: found → reserving     │
     │                               │                              │
     │                               │  예약 결과 수신              │
     │                               │──────────────────────────────▶│
     │                               │                   성공: 예약 정보 표시
     │                               │                   실패: 사유 표시
     │                               │                              │
     │◀──────────────────────────────│──────────────────────────────│
     │  "조건 수정" 탭               │  "재시도" 탭                  │
     │                               │◀─────────────────────────────│
     │                               │  상태: searching (재개)       │
```

### 8.2 각 화면에서의 상태별 표시

| 상태 | SearchScreen | MonitorScreen | ResultScreen |
|------|-------------|---------------|-------------|
| `idle` | 입력 폼 활성 | "대기 중" 표시, 중지 버튼 비활성 | 이전 결과 또는 빈 상태 |
| `searching` | 입력 폼 비활성 (읽기전용) | 실시간 조회 표시, 중지 버튼 활성 | - |
| `found` | - | 발견 알림 표시 | - |
| `reserving` | - | "예약 중..." 표시 | - |
| `success` | - | 성공 표시 (짧게) | 예약 성공 카드 |
| `failure` | - | 실패 표시 (짧게) | 예약 실패 카드 |

---

## 9. 반응형 및 접근성

### 9.1 반응형 레이아웃

| 화면 너비 | 레이아웃 조정 |
|----------|-------------|
| < 360dp (소형) | StationSelector의 출발역/도착역을 세로 배치, 글꼴 크기 축소 |
| 360~600dp (일반) | 기본 레이아웃 적용 |
| > 600dp (태블릿) | 좌우 패딩 증가, 카드 최대 너비 600dp 제한 |

### 9.2 접근성 (Accessibility)

| 항목 | 대응 |
|------|------|
| Semantics | 모든 인터랙티브 위젯에 `Semantics` 라벨 부여 |
| 색상 대비 | WCAG 2.1 AA 기준 충족 (텍스트 최소 4.5:1 대비율) |
| 터치 영역 | 최소 48x48dp 확보 |
| 상태 표현 | 색상만이 아닌, 아이콘+텍스트 삼중 표현으로 색맹 사용자 대응 |

---

## 10. 파일 구조 (예상)

```
lib/
├── main.dart
├── app.dart                          // MaterialApp, 테마, 네비게이션
│
├── core/
│   ├── theme/
│   │   ├── app_theme.dart            // ThemeData 정의
│   │   └── app_colors.dart           // 브랜드/시맨틱 컬러 상수
│   └── constants/
│       ├── stations.dart             // KTX 역 목록
│       └── spacing.dart              // 간격/크기 상수
│
├── presentation/
│   ├── screens/
│   │   ├── search_screen.dart        // 검색/설정 화면
│   │   ├── monitor_screen.dart       // 실시간 모니터 화면
│   │   └── result_screen.dart        // 예약 결과 화면
│   │
│   └── widgets/
│       ├── station_selector.dart     // StationSelector
│       ├── status_badge.dart         // StatusBadge
│       ├── train_card.dart           // TrainCard
│       └── log_tile.dart             // LogTile
│
└── data/
    └── models/
        ├── train.dart                // Train 모델
        ├── search_condition.dart     // SearchCondition 모델
        ├── reservation.dart          // Reservation 모델
        └── log_entry.dart            // LogEntry 모델
```

---

## 11. 위젯-기능 매핑 (Traceability)

| 위젯 | 기능 ID | 설명 |
|------|---------|------|
| StationSelector | FR-001 | 출발역/도착역 입력 |
| DatePicker / TimePicker | FR-001 | 날짜/시간 입력 |
| SwitchListTile (자동예약) | FR-002 | 자동 예약 ON/OFF 설정 |
| Slider (조회주기) | FR-002 | 조회 주기 설정 |
| "조회 시작" 버튼 | FR-001, FR-002 | 조회 시작 트리거 |
| StatusCard | FR-002 | 현재 모니터링 상태 표시 |
| StatsRow | FR-002 | 조회 통계 표시 |
| "중지" 버튼 | FR-002 | 자동 조회 중지 |
| LogListView / LogTile | FR-006 | 실시간 로그 표시 |
| ReservationCard (성공) | FR-003, FR-004 | 예약 성공 결과 표시 |
| ReservationCard (실패) | FR-003, FR-004 | 예약 실패 결과 표시 |
| LogExpansionTile | FR-006 | 전체 로그 펼치기/접기 |
| TrainCard | FR-001, FR-003 | 열차 정보 카드 |
| StatusBadge | FR-002, FR-004 | 상태 시각화 |
| BottomNavigationBar | - | 화면 간 네비게이션 |
