# ==========================================
# ⚠️ 중요: 이 파일은 현재 사용되지 않습니다
# ==========================================
#
# 현재 프로젝트는 Python 백엔드 없이 Flutter에서
# 코레일 API를 직접 호출합니다 (lib/data/services/korail_api.dart)
#
# 이 파일은 향후 백엔드 추가 시 또는 참고용으로 유지됩니다.
# 실제 사용 시 .env 파일을 생성하고 아래 값을 채워주세요.
# (.env 파일은 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다)
# ==========================================

# 코레일 계정 (하드코딩 금지)
KORAIL_ID=your_korail_id
KORAIL_PW=your_korail_pw

# TAGO 공공데이터 API 키 (data.go.kr에서 발급)
TAGO_API_KEY=your_tago_api_key

# 서버 설정
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS 허용 오리진 (쉼표 구분)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000
